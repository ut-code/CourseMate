{
  "name": "course-mate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:mobile": "cd mobile && npm run lint",
    "format:mobile": "cd mobile && npm run format",
    "prepare": "husky",
    "check-branch": "if [ $(git branch --show-current) = \"main\" ]; then echo \"Cannot make commit on main! aborting...\"; exit 1; fi"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.4"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
