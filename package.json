{
  "name": "course-mate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "check-branch": "if [ $(git branch --show-current) = \"main\" ]; then echo \"Cannot make commit on main! aborting...\"; exit 1; fi"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^9.10.0",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "lint-staged": {
    "web/**/*.{js,jsx,ts,tsx}": [
      "eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 --config web/.eslintrc.json"
    ],
    "server/**/*.{js,jsx,ts,tsx}": [
      "eslint --report-unused-disable-directives --max-warnings 0 --config server/eslint.config.mjs"
    ],
    "*.{js,jsx,ts,tsx}": [
      "prettier --write"
    ]
  }
}
