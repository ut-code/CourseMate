import {
  apiEndpoint,
  apiKey,
  authDomain,
  projectId,
  storageBucket,
  messagingSenderId,
  appId,
  measurementId,
  supabaseUrl,
  supabaseKey,
} from "@env";

export const API_KEY = apiKey;
export const AUTH_DOMAIN = authDomain;
export const PROJECT_ID = projectId;
export const STORAGE_BUCKET = storageBucket;
export const MESSAGING_SENDER_ID = messagingSenderId;
export const APP_ID = appId;
export const MEASUREMENT_ID = measurementId;

export const API_ENDPOINT = apiEndpoint;
export const SUPABASE_URL = supabaseUrl;
export const SUPABASE_KEY = supabaseKey;
